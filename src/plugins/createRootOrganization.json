{
  "pluginName": "createRootOrganization",
  "version": "1.0.0",
  "description": "Create a root organization structure with memory integration for infrastructure modeling",
  "executionContext": "server",
  "parameters": {
    "description": {
      "type": "string",
      "required": true,
      "description": "Description of the organization to create"
    }
  },
  "inlineCode": "async function createRootOrganization(description) {\n  try {\n    const response = await fetch('/api/vector-memory', {\n      method: 'POST',\n      headers: { 'Content-Type': 'application/json' },\n      body: JSON.stringify({\n        action: 'createOrganization',\n        description: description\n      })\n    });\n    \n    if (!response.ok) {\n      throw new Error(`HTTP error! status: ${response.status}`);\n    }\n    \n    const result = await response.json();\n    \n    if (!result.success) {\n      throw new Error(result.error || 'Failed to create organization');\n    }\n    \n    return {\n      success: true,\n      organization: result.organization,\n      description: description,\n      message: `Created root organization: ${description}`,\n      timestamp: new Date().toISOString()\n    };\n  } catch (error) {\n    return {\n      success: false,\n      error: error.message,\n      timestamp: new Date().toISOString()\n    };\n  }\n}",
  "dependencies": [],
  "timeout": 15000,
  "retries": 2,
  "metadata": {
    "category": "vector-memory",
    "tags": ["organization", "create", "root", "structure"],
    "author": "infrasim-core",
    "createdAt": "2024-01-01T00:00:00.000Z",
    "modifiable": true,
    "llmEnhanced": false
  }
}